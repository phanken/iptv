# IPTV Personal Render

Render: Build `npm install`, Start `npm start`.
Environment: `ADMIN_KEY`, `MONGODB_URI`.
Trang xem: `/`; quản trị: `/admin`; kiểm tra: `/health`.
Chỉ dùng stream/playlist bạn có quyền sử dụng.
